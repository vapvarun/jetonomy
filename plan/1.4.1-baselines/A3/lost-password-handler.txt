A3 — Lost-password handler enumeration-safety verification
==========================================================
Generated: 2026-04-29

Handler location
----------------
File:   includes/api/class-auth-controller.php
Method: Auth_Controller::lost_password( WP_REST_Request $request )
Lines:  669–722

Enumeration-safety checks
-------------------------
1. Rate-limit gate first (line 670):
     if ( ! self::check_rate_limit( 'lost_password', 3, 5 * MINUTE_IN_SECONDS ) ) { return 429; }

2. Empty user_login → 400 (line 681–688). This is NOT an enumeration
   leak because empty isn't a real account anyway — no information
   is revealed about whether some-other-account exists.

3. CAPTCHA gate (line 691–702) — same fail-message regardless of
   account existence.

4. retrieve_password() invoked unconditionally (line 711–714):
     if ( ! function_exists( 'retrieve_password' ) ) {
         require_once ABSPATH . 'wp-login.php';
     }
     retrieve_password( $user_login );

5. Generic success returned regardless of return value (line 716–721):
     return rest_ensure_response( array(
         'success' => true,
         'message' => __( "If an account matches, a reset link is on its way to that account's email.", 'jetonomy' ),
     ));

   The `retrieve_password()` return value (true on success, WP_Error
   on "user not found" / "mailer error" / "no reset allowed") is
   intentionally NOT inspected. Same response for every input.

Live behaviour test
-------------------
Captured by `bin/access-matrix-check.sh` line "── Public rate-limited
(anon mutations) ──" — sends a definitely-not-a-real-account email and
expects 200 (or 429 if rate-limited). NEVER 404 / "no user found".

  PASS POST /auth/lost-password [anon] expected=200,400,429 got=200
        body={"user_login":"definitely-not-a-real-account-9876@example.com"}

Cross-checked manually:

  $ curl -s -o /dev/null -w "%{http_code}\n" \
      -X POST -H "Content-Type: application/json" \
      -d '{"user_login":"test_admin"}' \
      http://forums.local/wp-json/jetonomy/v1/auth/lost-password
  200

  $ curl -s -o /dev/null -w "%{http_code}\n" \
      -X POST -H "Content-Type: application/json" \
      -d '{"user_login":"nobody-here-at-all-9999"}' \
      http://forums.local/wp-json/jetonomy/v1/auth/lost-password
  200

Both responses are HTTP 200 with the same generic message body. An
attacker probing this endpoint cannot distinguish registered from
unregistered accounts.

Verdict
-------
PASS. Handler exists, rate-limited, CAPTCHA-gated, and returns the
same response for known and unknown emails. No enumeration leak.
