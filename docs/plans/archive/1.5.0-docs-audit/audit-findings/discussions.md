# Audit fixes — discussions (8 findings)

## 1. [MAJOR] `/Users/varundubey/Local Sites/forums/app/public/wp-content/plugins/jetonomy/docs/website/discussions/05-drafts-scheduling.md` — inaccurate
- **Issue:** Doc claims Ctrl+S / Cmd+S saves a draft silently; no such keyboard handler exists.
- **Fix:** Delete the Ctrl+S/Cmd+S Tip blockquote at line 23. If a quick-save shortcut is desired, document Ctrl+Enter/Cmd+Enter as the SUBMIT shortcut (which clicks the primary button, i.e. whatever the split-button's current primary action is), not a silent draft save.
- **Evidence:** doc 05-drafts-scheduling.md:23 says 'pressing Ctrl+S ... saves silently in the background'. The ONLY composer keyboard shortcut in assets/js/composer.js is at lines 151-158: `if ( ( e.ctrlKey || e.metaKey ) && e.key === 'Enter' )` -> clicks `.jt-btn-fill` (submit). grep across composer.js and view.js shows no `=== 's'` / saveDraft keydown handler. A Ctrl+S press falls through to the browser's native Save-Page dialog.

## 2. [MAJOR] `/Users/varundubey/Local Sites/forums/app/public/wp-content/plugins/jetonomy/docs/website/discussions/01-creating-topics.md` — inaccurate
- **Issue:** Doc says Similar Topics detection 'runs entirely on the client against the search index - no additional page load'; it actually performs a debounced server-side REST search request.
- **Fix:** Replace the sentence at line 72 with: 'Similar Topics detection runs a lightweight, debounced search request (about 400ms after you stop typing) against the server search index, so it does not reload the page.' The claim is genuinely wrong, but cite view.js:3185-3221 as the evidence, not composer.js.
- **Evidence:** doc 01-creating-topics.md:72. The auditor cited composer.js:304, but that line is the SEARCH-PAGE instant-search (`.jt-search-page-input`), a different feature. The actual Similar-Topics implementation lives in assets/js/view.js:3185-3221: `searchSimilar()` builds `url = SIMILAR_API + '/search?q=' + encodeURIComponent( q ) + '&type=post'` (3194) then `fetch( url, { credentials: 'same-origin' } )` (3199), wired to the title input via a 400ms debounce (3218-3220). So it is a server-side GET /jetonomy/v1/search request per keystroke (debounced), not a client-only index lookup.

## 3. [MAJOR] `/Users/varundubey/Local Sites/forums/app/public/wp-content/plugins/jetonomy/docs/website/discussions/08-composer-features.md` — inaccurate
- **Issue:** Doc describes a per-space WYSIWYG/TinyMCE editor mode in Space Settings -> Editor with markdown<->HTML conversion; none of this exists.
- **Fix:** Remove the entire 'Markdown and WYSIWYG' section (169-178) including the two-input-modes claim, the Space Settings -> Editor reference, and the 'markdown is converted to HTML and vice versa on save' line. Replace with an accurate description of the single contenteditable toolbar. Also fix the What-You-Will-Learn bullet at line 17 ('markdown vs the WYSIWYG editor').
- **Evidence:** doc 08-composer-features.md:169-178. `grep -rln 'wysiwyg|tinymce|WYSIWYG|TinyMCE' includes/ templates/ assets/js/composer.js assets/js/view.js` returns ZERO hits. `grep 'input_mode|editor|markdown|wysiwyg' includes/models/class-space.php` returns nothing - no such space setting. Composer is a single contenteditable toolbar (templates/partials/composer.php / compose-fields.php fixed `data-cmd` buttons; composer.js header 'Simple contenteditable enhancement').

## 4. [MAJOR] `/Users/varundubey/Local Sites/forums/app/public/wp-content/plugins/jetonomy/docs/website/discussions/09-modals-confirmations.md` — inaccurate
- **Issue:** Developer-note code sample uses wrong jetonomyConfirm signature (object with message key), wrong red-button flag (destructive vs danger), and a nonexistent window.jetonomy.modals namespace.
- **Fix:** Rewrite the code sample to `await window.jetonomyConfirm('This cannot be undone.', { title: 'Delete this post?', confirmLabel: 'Delete', danger: true })`. Change 'The destructive: true flag' prose at line 170 to 'The danger: true flag'. Remove the final sentence claiming the functions are on window.jetonomy.modals - they are bare window globals.
- **Evidence:** doc 09-modals-confirmations.md:148-172. Code assets/js/jetonomy-modals.js:249 `window.jetonomyConfirm = function ( message, opts )` - message is the FIRST positional arg, not an object key. The red-button flag is `opts.danger` (line 126 `okBtn.className = ... ( opts.danger ? 'jt-btn-danger' : 'jt-btn-fill' )`; line 257 `danger: !! opts.danger`); the file header at line 17 documents `opts.danger?`. There is NO `destructive` key anywhere and NO `window.jetonomy.modals` namespace (the three globals are window.jetonomyConfirm/Alert/Prompt only).

## 5. [MINOR] `/Users/varundubey/Local Sites/forums/app/public/wp-content/plugins/jetonomy/docs/website/discussions/08-composer-features.md` — inaccurate
- **Issue:** Managed-by card template path is wrong: doc cites templates/parts/space-sidebar-managed-by.php but the file is templates/partials/managed-by-card.php.
- **Fix:** Update line 103 to: rendered from templates/partials/managed-by-card.php; theme override at theme/jetonomy/partials/managed-by-card.php.
- **Evidence:** doc 08-composer-features.md:103. `ls templates/parts/` -> 'No such file or directory'. The template exists at templates/partials/managed-by-card.php. So both the directory ('parts' vs 'partials') and filename ('space-sidebar-managed-by.php' vs 'managed-by-card.php') in the doc are wrong, and the theme-override path is consequently wrong too.

## 6. [MINOR] `/Users/varundubey/Local Sites/forums/app/public/wp-content/plugins/jetonomy/docs/website/discussions/01-creating-topics.md` — inaccurate
- **Issue:** Toolbar options table lists a separate 'Code block' (multi-line syntax-highlighting) button that does not exist; there is only one code button (inline </>).
- **Fix:** Remove the 'Code block' row from the toolbar table at line 50. Keep only the single inline-code '</>'  button row.
- **Evidence:** doc 01-creating-topics.md:46 (inline '< >') and :50 ('Code block - Multi-line code with syntax highlighting'). Code: templates/partials/compose-fields.php:158 and templates/partials/composer.php:34 each define a SINGLE `data-cmd="code"` button labelled `</>`. grep for code-block/codeblock/code_block in composer.js and templates/partials returns no matches. The toolbar buttons are bold, italic, code, link, quote, image - no separate code-block button.

## 7. [MINOR] `/Users/varundubey/Local Sites/forums/app/public/wp-content/plugins/jetonomy/docs/website/discussions/08-composer-features.md` — inaccurate
- **Issue:** Doc says mention dropdown shows 'up to 8 matching members'; the endpoint returns up to 10 and the JS renders all returned matches uncapped.
- **Fix:** Change 'up to 8 matching members' to 'up to 10 matching members' at line 49.
- **Evidence:** doc 08-composer-features.md:49. Code includes/api/class-users-controller.php:150 and :163 both use `'number' => 10` for the /users/suggest query. assets/js/composer.js:762 renderMatches() does `matches.forEach( function ( m, i ) {...} )` with no slice/cap. So the visible maximum is 10, not 8.

## 8. [MINOR] `/Users/varundubey/Local Sites/forums/app/public/wp-content/plugins/jetonomy/docs/website/discussions/05-drafts-scheduling.md` — inaccurate
- **Issue:** Doc frames scheduled publishing as 'using WordPress Cron (WP-Cron)'; the primary scheduler is Action Scheduler and WP-Cron is explicitly migrated away from.
- **Fix:** Reframe lines 71-77 to say scheduled posts are published via Action Scheduler (the same background queue WooCommerce uses), checked hourly, with WP-Cron as a legacy fallback. Drop or soften the 'depends on when the next visitor triggers cron' and the crontab-precision note, since AS has its own runner. Keep the hourly cadence statement - that is correct.
- **Evidence:** doc 05-drafts-scheduling.md:71-77. Code includes/class-cron.php: line 32 `'jetonomy_publish_scheduled' => HOUR_IN_SECONDS` (hourly cadence correct); line 49 hooks ensure_scheduled() on `action_scheduler_init`; ensure_scheduled() (85-107) calls `as_schedule_recurring_action(...)` (105) and on first run wipes legacy WP-Cron via `wp_clear_scheduled_hook()` (91-95) setting jetonomy_cron_as_migrated. So Action Scheduler owns the hook; WP-Cron is only a legacy/fallback path. The 'uses WordPress Cron (WP-Cron)' framing and the 'depends on next site visitor triggering a cron run' explanation are inaccurate for the primary AS path (AS runs its own queue runner, not purely page-view-gated).
