// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'M01 — Open global moderation queue', () => {
	test.skip( true, 'Not yet implemented — Phase 2' );

	test( 'Open global moderation queue', async ( { page } ) => {
		// Priority: P0
		// Actor: moderator
		// TODO: Implement per usability test plan
	} );
} );
