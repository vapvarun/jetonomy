// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'B01 — First login → community redirect', () => {
	test.skip( true, 'Not yet implemented — Phase 2' );

	test( 'First login → community redirect', async ( { page } ) => {
		// Priority: P0
		// Actor: registered
		// TODO: Implement per usability test plan
	} );
} );
