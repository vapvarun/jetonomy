// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'B02 — Edit own profile first time', () => {
	test.skip( true, 'Not yet implemented — Phase 5' );

	test( 'Edit own profile first time', async ( { page } ) => {
		// Priority: P1
		// Actor: registered
		// TODO: Implement per usability test plan
	} );
} );
