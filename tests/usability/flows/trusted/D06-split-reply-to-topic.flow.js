// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'D06 — Split reply into new topic', () => {
	test.skip( true, 'Not yet implemented — Phase 7' );

	test( 'Split reply into new topic', async ( { page } ) => {
		// Priority: P2
		// Actor: trusted
		// TODO: Implement per usability test plan
	} );
} );
