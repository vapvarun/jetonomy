// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'GA10 — User list + ban/trust control', () => {
	test.skip( true, 'Not yet implemented — Phase 2' );

	test( 'User list + ban/trust control', async ( { page } ) => {
		// Priority: P0
		// Actor: site-admin
		// TODO: Implement per usability test plan
	} );
} );
