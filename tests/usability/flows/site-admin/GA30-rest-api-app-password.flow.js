// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'GA30 — REST API via application password', () => {
	test.skip( true, 'Not yet implemented — Phase 5' );

	test( 'REST API via application password', async ( { page } ) => {
		// Priority: P1
		// Actor: site-admin
		// TODO: Implement per usability test plan
	} );
} );
