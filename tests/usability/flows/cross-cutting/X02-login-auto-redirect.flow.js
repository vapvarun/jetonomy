// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'X02 — WordPress login auto-redirect', () => {
	test.skip( true, 'Not yet implemented — Phase 5' );

	test( 'WordPress login auto-redirect', async ( { page } ) => {
		// Priority: P1
		// Actor: cross-cutting
		// TODO: Implement per usability test plan
	} );
} );
