// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'X03 — oEmbed rendering in post body', () => {
	test.skip( true, 'Not yet implemented — Phase 5' );

	test( 'oEmbed rendering in post body', async ( { page } ) => {
		// Priority: P1
		// Actor: cross-cutting
		// TODO: Implement per usability test plan
	} );
} );
