// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'X01 — BuddyPress profile tab merge', () => {
	test.skip( true, 'Not yet implemented — Phase 7' );

	test( 'BuddyPress profile tab merge', async ( { page } ) => {
		// Priority: P2
		// Actor: cross-cutting
		// TODO: Implement per usability test plan
	} );
} );
