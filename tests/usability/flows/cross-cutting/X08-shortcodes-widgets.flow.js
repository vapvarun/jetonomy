// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'X08 — Shortcodes and widgets', () => {
	test.skip( true, 'Not yet implemented — Phase 7' );

	test( 'Shortcodes and widgets', async ( { page } ) => {
		// Priority: P2
		// Actor: cross-cutting
		// TODO: Implement per usability test plan
	} );
} );
