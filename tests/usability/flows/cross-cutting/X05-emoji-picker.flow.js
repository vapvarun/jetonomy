// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'X05 — Emoji picker in reply editor', () => {
	test.skip( true, 'Not yet implemented — Phase 7' );

	test( 'Emoji picker in reply editor', async ( { page } ) => {
		// Priority: P2
		// Actor: cross-cutting
		// TODO: Implement per usability test plan
	} );
} );
