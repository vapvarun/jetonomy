// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'X04 — Drag-drop image upload in editor', () => {
	test.skip( true, 'Not yet implemented — Phase 5' );

	test( 'Drag-drop image upload in editor', async ( { page } ) => {
		// Priority: P1
		// Actor: cross-cutting
		// TODO: Implement per usability test plan
	} );
} );
