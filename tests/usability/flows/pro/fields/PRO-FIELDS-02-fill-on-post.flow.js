// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'PRO-FIELDS-02 — Fill custom field on new post', () => {
	test.skip( true, 'Not yet implemented — Phase 3' );

	test( 'Fill custom field on new post', async ( { page } ) => {
		// Priority: P0
		// Actor: pro-fields
		// TODO: Implement per usability test plan
	} );
} );
