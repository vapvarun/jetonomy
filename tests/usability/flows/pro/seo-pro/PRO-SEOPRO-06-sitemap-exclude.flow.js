// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'PRO-SEOPRO-06 — Exclude space from sitemap', () => {
	test.skip( true, 'Not yet implemented — Phase 6' );

	test( 'Exclude space from sitemap', async ( { page } ) => {
		// Priority: P1
		// Actor: pro-seo-pro
		// TODO: Implement per usability test plan
	} );
} );
