// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'PRO-ANALYTICS-08 — Permission denied non-admin', () => {
	test.skip( true, 'Not yet implemented — Phase 3' );

	test( 'Permission denied non-admin', async ( { page } ) => {
		// Priority: P0
		// Actor: pro-analytics
		// TODO: Implement per usability test plan
	} );
} );
