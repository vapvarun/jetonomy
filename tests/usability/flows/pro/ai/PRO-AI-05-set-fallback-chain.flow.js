// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'PRO-AI-05 — Set default provider + fallback', () => {
	test.skip( true, 'Not yet implemented — Phase 6' );

	test( 'Set default provider + fallback', async ( { page } ) => {
		// Priority: P1
		// Actor: pro-ai
		// TODO: Implement per usability test plan
	} );
} );
