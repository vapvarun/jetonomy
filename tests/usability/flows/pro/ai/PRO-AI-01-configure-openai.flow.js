// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'PRO-AI-01 — Admin configure OpenAI', () => {
	test.skip( true, 'Not yet implemented — Phase 3' );

	test( 'Admin configure OpenAI', async ( { page } ) => {
		// Priority: P0
		// Actor: pro-ai
		// TODO: Implement per usability test plan
	} );
} );
