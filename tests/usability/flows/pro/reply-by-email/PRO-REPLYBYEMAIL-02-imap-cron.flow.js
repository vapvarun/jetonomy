// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'PRO-REPLYBYEMAIL-02 — IMAP cron polls inbox', () => {
	test.skip( true, 'Not yet implemented — Phase 3' );

	test( 'IMAP cron polls inbox', async ( { page } ) => {
		// Priority: P0
		// Actor: pro-reply-by-email
		// TODO: Implement per usability test plan
	} );
} );
