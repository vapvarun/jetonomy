// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'PRO-REPLYBYEMAIL-01 — Outbound email gets Reply-To token', () => {
	test.skip( true, 'Not yet implemented — Phase 3' );

	test( 'Outbound email gets Reply-To token', async ( { page } ) => {
		// Priority: P0
		// Actor: pro-reply-by-email
		// TODO: Implement per usability test plan
	} );
} );
