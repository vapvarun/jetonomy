// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'A04 — Read a single post + replies', () => {
	test.skip( true, 'Not yet implemented — Phase 2' );

	test( 'Read a single post + replies', async ( { page } ) => {
		// Priority: P0
		// Actor: anonymous
		// TODO: Implement per usability test plan
	} );
} );
