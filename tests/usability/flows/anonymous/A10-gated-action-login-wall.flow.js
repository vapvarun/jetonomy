// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'A10 — Attempt gated action → login wall', () => {
	test.skip( true, 'Not yet implemented — Phase 2' );

	test( 'Attempt gated action → login wall', async ( { page } ) => {
		// Priority: P0
		// Actor: anonymous
		// TODO: Implement per usability test plan
	} );
} );
