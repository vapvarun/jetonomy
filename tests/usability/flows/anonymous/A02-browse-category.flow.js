// @ts-check
const { test } = require( '@playwright/test' );

test.describe( 'A02 — Browse a public category', () => {
	test.skip( true, 'Not yet implemented — Phase 2' );

	test( 'Browse a public category', async ( { page } ) => {
		// Priority: P0
		// Actor: anonymous
		// TODO: Implement per usability test plan
	} );
} );
