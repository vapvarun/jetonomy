flow_id: "PRO-WEBHOOKS-01"
title: "Admin create webhook"
source: "design-spec"
priority: P0
actor: pro-webhooks

user_story: |
  As an admin, I create a webhook subscription via CLI and expect it to
  be stored in the webhooks table and readable back.

expectations:
  flow_completes_without_error: true
  webhook_created_in_db: true
  webhook_readable_via_cli: true
