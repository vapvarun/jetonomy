flow_id: "PRO-WEBHOOKS-07"
title: "HMAC-SHA256 signature"
source: "design-spec"
priority: P1
actor: pro-webhooks

user_story: |
  As a system, when a webhook has a secret configured, deliveries should
  include an HMAC-SHA256 signature header for verification.

expectations:
  flow_completes_without_error: true
  signature_header_present: true
