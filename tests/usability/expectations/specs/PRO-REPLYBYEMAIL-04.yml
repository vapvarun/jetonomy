flow_id: "PRO-REPLYBYEMAIL-04"
title: "Token validation"
source: "design-spec"
priority: P0
actor: pro-reply-by-email

user_story: |
  As a system, a generated token should decode to the correct post_id
  and user_id, and invalid tokens should be rejected.

expectations:
  flow_completes_without_error: true
  valid_token_decodes: true
  invalid_token_rejected: true
