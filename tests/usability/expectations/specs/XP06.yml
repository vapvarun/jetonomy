flow_id: "XP06"
title: "User registers → webhooks + digest + badges"
source: "design-spec"
priority: P1
actor: composite

user_story: |
  As a system, when a new user registers, webhooks should fire, digest
  preferences should be seeded, and badge evaluation should run.

expectations:
  flow_completes_without_error: true
  webhook_dispatched: true
