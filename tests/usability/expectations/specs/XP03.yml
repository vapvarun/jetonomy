flow_id: "XP03"
title: "User reacts → analytics"
source: "design-spec"
priority: P1
actor: composite

user_story: |
  As a system, when a user reacts to a post, the analytics extension
  should record the engagement metric increment.

expectations:
  flow_completes_without_error: true
  reaction_recorded: true
