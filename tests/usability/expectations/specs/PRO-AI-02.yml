flow_id: "PRO-AI-02"
title: "User submits post → spam verdict"
source: "design-spec"
priority: P0
actor: pro-ai

user_story: |
  As a system, when a user submits a post, the AI spam detector should
  evaluate it and return a verdict (requires provider configured).

expectations:
  flow_completes_without_error: true
  spam_verdict_returned: true
