flow_id: "PRO-WEBHOOKS-03"
title: "Event: post.created end-to-end"
source: "design-spec"
priority: P0
actor: pro-webhooks

user_story: |
  As a system, the full post.created webhook lifecycle works end-to-end:
  webhook created, post created, delivery logged with correct payload.

expectations:
  flow_completes_without_error: true
  delivery_logged: true
  payload_contains_event: true
