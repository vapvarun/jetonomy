flow_id: "PRO-WEBHOOKS-08"
title: "Enable/disable webhook"
source: "design-spec"
priority: P1
actor: pro-webhooks

user_story: |
  As an admin, I can disable and re-enable a webhook via CLI, and the
  enabled flag toggles correctly in the database.

expectations:
  flow_completes_without_error: true
  disable_sets_enabled_0: true
  enable_sets_enabled_1: true
