flow_id: "PRO-REPLYBYEMAIL-03"
title: "Inbound webhook receives email"
source: "design-spec"
priority: P0
actor: pro-reply-by-email

user_story: |
  As a system, an inbound email webhook should process the email body,
  validate the token, and create a reply on the correct post.

expectations:
  flow_completes_without_error: true
  reply_created: true
