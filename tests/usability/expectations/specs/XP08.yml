flow_id: "XP08"
title: "Space loads → seo + white-label + fields"
source: "design-spec"
priority: P0
actor: composite

user_story: |
  As a visitor, loading a space page should render SEO meta tags,
  white-label branding, and custom fields from their respective
  extensions.

expectations:
  flow_completes_without_error: true
  no_console_errors: true
  seo_meta_rendered: true
  branding_rendered: true
