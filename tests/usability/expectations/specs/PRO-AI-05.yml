flow_id: "PRO-AI-05"
title: "Set default provider + fallback"
source: "design-spec"
priority: P1
actor: pro-ai

user_story: |
  As an admin, I configure a provider fallback chain and expect the
  order to persist in settings.

expectations:
  flow_completes_without_error: true
  fallback_chain_persists: true
