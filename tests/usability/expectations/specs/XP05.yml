flow_id: "XP05"
title: "Create conversation → web-push"
source: "design-spec"
priority: P0
actor: composite

user_story: |
  As a system, creating a conversation should dispatch a web-push
  notification to the recipient.

expectations:
  flow_completes_without_error: true
  conversation_created: true
