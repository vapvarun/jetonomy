flow_id: "XP07"
title: "Flag → advanced-mod → webhooks → analytics"
source: "design-spec"
priority: P1
actor: composite

user_story: |
  As a system, flagging content should trigger advanced moderation
  evaluation, webhook dispatch, and analytics tracking.

expectations:
  flow_completes_without_error: true
  flag_row_created: true
  webhook_dispatched: true
