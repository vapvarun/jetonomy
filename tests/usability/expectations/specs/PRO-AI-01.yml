flow_id: "PRO-AI-01"
title: "Admin configure OpenAI"
source: "design-spec"
priority: P0
actor: pro-ai

user_story: |
  As an admin, I visit the AI settings tab and expect to see OpenAI
  provider configuration fields (API key, model).

expectations:
  flow_completes_without_error: true
  no_console_errors: true
  openai_fields_visible: true
