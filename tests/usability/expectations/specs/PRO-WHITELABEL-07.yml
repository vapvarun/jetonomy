flow_id: "PRO-WHITELABEL-07"
title: "Reset to defaults"
source: "design-spec"
priority: P1
actor: pro-white-label

user_story: |
  As an admin, I reset all branding to defaults and expect all custom
  values to be cleared.

expectations:
  flow_completes_without_error: true
  custom_values_cleared: true
