flow_id: "B03"
title: "Browse space directory"
source: "design-spec"
priority: P0
actor: registered

user_story: |
  As a registered user (bob), when I visit /community/, I expect to see
  the community home page with space cards/listings visible, just as an
  anonymous user would but with my logged-in state reflected.

expectations:
  flow_completes_without_error: true
  no_console_errors: true
  community_container_visible: true
  space_listings_present: true
