flow_id: "PRO-REPLYBYEMAIL-08"
title: "Admin configure provider"
source: "design-spec"
priority: P1
actor: pro-reply-by-email

user_story: |
  As an admin, I visit the reply-by-email settings tab and expect to
  see provider/mode configuration fields.

expectations:
  flow_completes_without_error: true
  no_console_errors: true
  provider_field_visible: true
