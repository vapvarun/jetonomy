flow_id: "PRO-ANALYTICS-06"
title: "Export CSV"
source: "design-spec"
priority: P1
actor: pro-analytics

user_story: |
  As an admin on the analytics dashboard, I expect an Export CSV button
  that triggers a file download when clicked.

expectations:
  flow_completes_without_error: true
  no_console_errors: true
  export_button_visible: true
  max_clicks_to_goal: 1
