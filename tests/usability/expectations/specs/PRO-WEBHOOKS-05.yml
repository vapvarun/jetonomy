flow_id: "PRO-WEBHOOKS-05"
title: "Failure increments fail_count"
source: "design-spec"
priority: P1
actor: pro-webhooks

user_story: |
  As a system, when a webhook delivery fails, the fail_count column
  should increment on the webhook row.

expectations:
  flow_completes_without_error: true
  fail_count_incremented: true
