flow_id: "XP04"
title: "Poll vote → webhooks → analytics"
source: "design-spec"
priority: P1
actor: composite

user_story: |
  As a system, when a user votes on a poll, the webhooks extension should
  fire a poll.voted event.

expectations:
  flow_completes_without_error: true
  webhook_dispatched: true
